[Download](https://hyperledger-org.bintray.com/aries/) and extract static libraries required for iOS and Andoroid projects.

The final folder structure from the root of the repo should look like this:

```
libs/
  ios/
  android/
```